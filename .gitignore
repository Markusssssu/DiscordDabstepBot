### Rust ###
# Папка с результатами компиляции (всегда игнорируем)
target/

# Для бинарных проектов (ботов) Cargo.lock ДОЛЖЕН быть в git.
# Если вы пишете библиотеку — закомментируйте строку ниже.
# !Cargo.lock

# Временные файлы и бэкапы
**/*.rs.bk
*.pdb

### Окружение и секреты (Важно!) ###
# Игнорируем файл с токенами и ключами
.env

# Но разрешаем версионность шаблона (чтобы другие знали, какие переменные нужны)
!.env.example

### IDE & OS ###
.vscode/
.idea/
.DS_Store
Thumbs.db
